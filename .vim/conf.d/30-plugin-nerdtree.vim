let NERDTreeShowHidden=1 " Show hidden files by default
let NERDTreeQuitOnOpen=1 " Automatically close NERDTree when you open a file
let NERDTreeMinimalUI = 1 " Hide the top bar
let NERDTreeDirArrows = 1 " Show arrows for folders
let NERDTreeAutoDeleteBuffer = 1 " Delete buffers when file is deleted from disk

nmap <C-b> :NERDTreeToggle<CR>
nmap <S-b> :NERDTreeFind<CR>
